namespace Cake.ImageMagick
{
    public enum LayersMethod
    {
        compareAny,
        compareClear,
        compareOverlay,
        coalesce,
        composite,
        dispose,
        flatten,
        merge,
        mosaic,
        optimize,
        optimizeFrame,
        optimizePlus,
        optimizeTransparency,
        removeDups,
        remoevZero,
        trimBounds
    }
}
